import { login } from "./login";
import { register } from "./register";
import { getUserByUsername } from "./getUserByUsername";

export const server = {
    login,
    register,
    getUserByUsername,
};