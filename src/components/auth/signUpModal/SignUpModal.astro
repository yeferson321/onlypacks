---
import type { Translation } from '@/i18n/config';
import SignUpForm from './SignUpForm.astro';

interface Props {
	translations: Translation.LoginModal;
};

const { translations } = Astro.props;
---
	
<h2 id="login-modal-title" class="text-center text-2xl font-medium text-neutral-800">{translations.signUp.title}</h2>

<p class="mt-2 text-center text-base text-neutral-800">{translations.signUp.subtitle.text} <button type="button" class="underline font-medium" data-multistep-target="sign-in" aria-label="">{translations.signUp.subtitle.action}</button></p>

<SignUpForm translations={translations.form.signUp}/>

<script>
	// const multiStepElement = document.querySelector<HTMLDivElement>("[data-multi-step]");
	// const signUpButton = document.querySelector<HTMLButtonElement>("[data-multistep-view='sign-in']");

	// const hola = (signUpButton: HTMLButtonElement) => {
	// 	const view = signUpButton.dataset.multistepView;

	// 	console.log("view", view);

    // 	multiStepElement?.dispatchEvent(new CustomEvent("multi-step:change", { 
    // 	    detail: { view: view } })
    // 	);
	// }

	// signUpButton?.addEventListener("click", () => hola(signUpButton));
</script>