---
import '@/styles/global.css';
import type { Profile } from '@/types/ui';
import type { Translation, LocaleCode } from '@/i18n/config';

import ModalLanguage from '@/components/ModalLanguage.astro';
import LoginModal from '@/components/registerLogin/LoginModal.astro';
import IconHeart from '@/icons/outline/IconHeart.svg';

interface Props {
	translations: Translation;
	locale: LocaleCode;
	data: Profile;
};

const { translations, locale, data } = Astro.props;
---

<!doctype html>
<html lang={locale}>
	<head>
		<!-- <ClientRouter /> -->
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Basics</title>
	</head>
	<body>

		<h2>Profile: {translations.about.title} locale: {locale}</h2>
		<h3>username: {data.username}</h3>
		<p>name: {data.name}</p>
		<p>isVerified {data.isVerified}</p>

		<button type="button" class="p-2.5 rounded-full bg-blue-500/20 hover:bg-blue-500/30 transition-colors duration-300 ease-in-out" data-modal-open="language-modal" aria-label="Abrir modal de cambio de idioma" aria-haspopup="dialog" aria-expanded="false" aria-controls="language-modal">
			<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" width="26" height="26" class="stroke-blue-500">
			  	<path stroke-linecap="round" stroke-linejoin="round" d="m10.5 21 5.25-11.25L21 21m-9-3h7.5M3 5.621a48.474 48.474 0 0 1 6-.371m0 0c1.12 0 2.233.038 3.334.114M9 5.25V3m3.334 2.364C11.176 10.658 7.69 15.08 3 17.502m9.334-12.138c.896.061 1.785.147 2.666.257m-4.589 8.495a18.023 18.023 0 0 1-3.827-5.802" />
			</svg>
		</button>

		<ModalLanguage url={data.username.toLocaleLowerCase()}/>

		<button type="button" class="p-2.5 rounded-full bg-blue-500/20 hover:bg-blue-500/30 transition-colors duration-300 ease-in-out" data-modal-open="login-modal" aria-label="Abrir modal de inicio de sesión" aria-haspopup="dialog" aria-expanded="false" aria-controls="login-modal">
			<IconHeart stroke-width="2" width="26" height="26" class="stroke-blue-500"/>
		</button>

		<LoginModal translations={translations.loginModal}/>

		<main class="scroll-container">
			<slot />
		</main>

	</body>
</html>

<style>
	@import url('https://fonts.googleapis.com/css2?family=Figtree:wght@300..900&family=Righteous&display=swap');

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;

		font-family: "Figtree", sans-serif;
  		font-optical-sizing: auto;
  		font-weight: 400;
  		font-style: normal;
	}
</style>


<!-- <Profile profile={data} />
		 -->
		<!-- <ProfileTabs username={username} tab={tab}/> -->

		<!-- <h2 class="px-8 mt-8 mb-4 text-3xl font-medium text-neutral-800">Preguntas frecuentes</h2>
		 
		<Accordion accordionId="questions" className="px-8">

			{questions.map(({ question, answer }, index) => (

	  		  	<div class="border-b border-neutral-300" data-state={index === 0 ? "open" : "closed"} data-accordion-item>
					
					<h3 id={`accordion-heading-questions-${index + 1}`}>
    		    		<button type="button" class="flex items-center justify-between gap-3 w-full py-4 text-xl font-medium text-neutral-800" data-accordion-button aria-expanded={index === 0 ? "true" : "false"} aria-controls={`accordion-body-questions-${index + 1}`}>
						
    		    		    <span>{question}</span>
						
    		    		    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" class="w-6 h-6 shrink-0 stroke-neutral-600" data-accordion-icon>
    		    		        <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
    		    		    </svg>
						
    		    		</button>
					</h3>

    		    	<div id={`accordion-body-questions-${index + 1}`} data-accordion-body aria-labelledby={`accordion-heading-questions-${index + 1}`} inert={index !== 0} aria-hidden={index !== 0 ? "true" : "false"}>
    		    	    <p class="mb-4 text-base text-neutral-800">{answer}</p>
    		    	</div>
			
    			</div>

	  		))}

		</Accordion> -->

						
			<!-- <div id={`profile-historia-${index + 1}`} class="relative w-full h-full shrink-0" aria-hidden={index === 0 ? "false" : "true"} data-carousel-item> 
			
				<img src={storie.url} width="326" height="500" loading={index === 0 ? "eager" : "lazy"} fetchpriority={index === 0 ? "high" : "low"} decoding={index === 0 ? "auto" : "async"} crossorigin="anonymous" class="w-full h-125 object-cover object-[50%_50%]" alt={`Historia ${index + 1} de ${stories.length}`} />
				<div class="absolute z-20 inset-x-0 bottom-0 h-70 backdrop-blur-[0.5px]" style={`background: linear-gradient(0deg, rgba(${storie.avgColor.r},${storie.avgColor.g},${storie.avgColor.b},1), rgba(${storie.avgColor.r},${storie.avgColor.g},${storie.avgColor.b},1) 50%, transparent 100%)`}></div>
				
			</div>  -->
			

			<!-- <div id="accordion-collapse-heading-1" class="border-b border-neutral-300">
					
    		    <button type="button" class="flex items-center justify-between gap-3 w-full py-5 text-xl font-medium text-neutral-800" aria-expanded="true" aria-controls="accordion-collapse-body-1">
				
    		        <span>Mac and iPhone</span>
				
    		        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" class="w-6 h-6 shrink-0 stroke-neutral-600" data-accordion-icon>
    		            <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
    		        </svg>
				
    		    </button>
			
    		    <div id="accordion-collapse-body-1" class="accordion-content" >
    		        <p class="mb-5 text-base text-neutral-800">Answer calls or messages from your iPhone directly on your Mac.</p>
    		    </div>
			
    		</div>
		
    		<div id="accordion-collapse-heading-2" class="">
			
    		    <button type="button" class="flex items-center justify-between gap-3 w-full py-5 text-xl font-medium text-neutral-800">
				
    		        <span>Mac and Apple Watch</span>
				
    		        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" class="w-6 h-6 shrink-0 stroke-neutral-600" data-accordion-icon>
    		            <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
    		        </svg>
				
    		    </button>
			
    		    <div class="accordion-content">
    		        <p class="mb-5 text-base text-neutral-800">Automatically log in to your Mac when you’re wearing your Apple Watch with Auto Unlock. No password t⁠y⁠p⁠i⁠n⁠g⁠ ⁠r⁠e⁠q⁠u⁠i⁠r⁠e⁠d⁠.</p>
    		    </div>
			
    		</div> -->
		