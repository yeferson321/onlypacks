---
// export async function getStaticPaths() {
//   	return [
//   	  	// { params: { /* required */ }, props: { /* optional */ } },
//   	  	{ params: { locale: 'es' } }, // [post] is the parameter
//   	  	{ params: { locale: 'en' } }, // must match the file name
//   	  	// ...
//   	];
// }

// export function getI18n(locale: string) {
//   	return import(`../../i18n/${locale}.json`);
// }

// const messages = await getI18n("es");

export const prerender = false;

import MainLayout from '@/layouts/MainLayout.astro';

const { locale, username, tab } = Astro.params;

console.log(Astro.preferredLocale, Astro.preferredLocaleList, Astro.currentLocale)
---

<MainLayout locale={locale} username={username} tab={tab}>
	<h1>Current messages: {Astro.preferredLocale} locale</h1>
</MainLayout>